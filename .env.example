# PostgreSQL bağlantısı — kendi bilgilerinizle değiştirin
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/finecrm?schema=public"

# Auth.js (NextAuth) oturum imzalama anahtarı — üretin: `openssl rand -base64 32`
AUTH_SECRET="degistirin-guclu-bir-rastgele-anahtar"

NOTIFICATION_FROM="no-reply@finecrm.local"

# Gerçek e-posta (Nodemailer) — SMTP_HOST tanımlıysa gerçek gönderim yapılır,
# aksi halde e-postalar terminale loglanarak simüle edilir.
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER=""
# SMTP_PASS=""
